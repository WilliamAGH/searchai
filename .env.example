# Frontend
VITE_CONVEX_URL=

# Convex CLI local deployment target (optional for local CLI usage)
CONVEX_DEPLOYMENT=

# Convex runtime variables (set via `npx convex env set`, shown here for reference)
# OPENROUTER_API_KEY=
# SERP_API_KEY=
# LLM_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
# LLM_HEALTHCHECK=
# LLM_HEALTHCHECK_TIMEOUT_MS=
# SITE_URL=

# Trust proxy headers (X-Forwarded-For, etc.) for rate limiting
# Set to "1" if deployed behind a trusted proxy (e.g. Cloudflare, AWS ALB)
# CONVEX_TRUST_PROXY=

# Optional: Restrict CORS for Convex HTTP export/publish routes.
# Comma-separated list of allowed origins. Use "*" (default) to allow all.
# Example (dev): CONVEX_ALLOWED_ORIGINS="http://localhost:5173,https://dev.search-ai.io"
# Example (prod): CONVEX_ALLOWED_ORIGINS="https://search-ai.io,https://dev.search-ai.io"
# Note: Set this with `npx convex env set CONVEX_ALLOWED_ORIGINS "..."` per environment.
# CONVEX_ALLOWED_ORIGINS=
