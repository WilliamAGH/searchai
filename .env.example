# Frontend
VITE_CONVEX_URL=

# Convex CLI local deployment target (optional for local CLI usage)
CONVEX_DEPLOYMENT=

# Convex runtime variables (set via `npx convex env set`, shown here for reference)
# OPENROUTER_API_KEY=
# SERP_API_KEY=
# LLM_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
# LLM_VISION_MODEL=
# LLM_HEALTHCHECK=
# LLM_HEALTHCHECK_TIMEOUT_MS=
# SITE_URL=
# AGENT_SIGNING_KEY=
# DEBUG_HTTP=

# Trust proxy headers (X-Forwarded-For, etc.) for rate limiting
# Set to "1" if deployed behind a trusted proxy (e.g. Cloudflare, AWS ALB)
# CONVEX_TRUST_PROXY=

# CORS allowlist for Convex HTTP routes (required for browser clients).
# Comma-separated list of allowed origins. Wildcard subdomains are supported as `*.example.com`.
# For local dev, include at least one Vite origin (e.g. http://localhost:5173). Common dev/preview
# ports are auto-added server-side to reduce friction.
#
# Note: Set this with `npx convex env set CONVEX_ALLOWED_ORIGINS "..."` per environment.
# Example (dev): CONVEX_ALLOWED_ORIGINS="http://localhost:5173"
# Example (prod): CONVEX_ALLOWED_ORIGINS="https://researchly.fyi,https://dev.researchly.fyi"
# CONVEX_ALLOWED_ORIGINS=
