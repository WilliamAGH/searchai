/**
 * Hook to provide service instances.
 * Retained as a stub for future dependency injection if needed.
 */
export function useServices(_convexUrl?: string): Record<string, never> {
  return {};
}
